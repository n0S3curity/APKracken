# Frontend environment. Copy to .env for local (non-Docker) development.

# Backend address the Vite dev server proxies /api to (server-side).
VITE_PROXY_TARGET=http://localhost:3002

# Optional: set an absolute API origin for the browser. Leave empty to use the
# same-origin /api path (proxied in dev).
VITE_API_BASE_URL=
