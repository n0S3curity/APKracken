# Backend environment. Copy to .env for local (non-Docker) development.
# In Docker, docker-compose injects DATABASE_URL pointing at the `db` service.
DATABASE_URL=postgresql://open_kritt:open_kritt_password@localhost:5432/open_kritt?schema=public
BACKEND_PORT=3002
NODE_ENV=development
